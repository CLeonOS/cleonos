name: Build CLeonOS

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      menuconfig_preset:
        description: "menuconfig preset (full/minimal/dev)"
        type: choice
        options:
          - full
          - minimal
          - dev
        required: false
        default: "full"
      menuconfig_overrides:
        description: "Optional extra Kconfig args, e.g. --set CLEONOS_CLKS_ENABLE_AUDIO=OFF"
        required: false
        default: ""

permissions:
  contents: read

jobs:
  build-os:
    runs-on: ubuntu-latest
    env:
      MENUCONFIG_PRESET: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.menuconfig_preset || 'full' }}
      MENUCONFIG_OVERRIDES: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.menuconfig_overrides || '' }}
      NO_COLOR: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Prepare submodules
        shell: bash
        run: |
          set -euo pipefail
          git submodule sync --recursive
          git submodule update --init --recursive --force
          if [[ ! -f limine/bootstrap && ! -f limine/configure ]]; then
            git submodule deinit -f limine || true
            rm -rf limine
            git submodule update --init --recursive --force limine
          fi
          test -f limine/bootstrap -o -f limine/configure

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            binutils \
            git \
            gawk \
            python3 \
            tar \
            xorriso \
            mtools \
            autoconf \
            automake \
            libtool \
            pkg-config \
            nasm \
            ninja-build \
            gcc-mingw-w64-x86-64 \
            kconfig-frontends-nox

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Generate Kconfig
        shell: bash
        run: |
          set -euo pipefail
          make defconfig \
            MENUCONFIG_PRESET="${MENUCONFIG_PRESET}" \
            MENUCONFIG_ARGS="--defaults ${MENUCONFIG_OVERRIDES}"
          echo "Kconfig generated with preset=${MENUCONFIG_PRESET}"

      - name: Build ISO with Ninja
        run: |
          make ninja-gen \
            CC=gcc \
            KERNEL_CXX=g++ \
            LD=ld \
            NM=nm \
            MENUCONFIG_PRESET="${MENUCONFIG_PRESET}" \
            MENUCONFIG_ARGS="${MENUCONFIG_OVERRIDES}"
          ninja -f build.ninja clboot-iso -j"$(nproc)"

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cleonos-clboot-iso
          path: build/CLeonOS-CLBoot-x86_64.iso
          if-no-files-found: error
