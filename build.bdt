[module]
name = cleonos-root
type = meta
languages = c, cpp, asm, rust

[sources]
scan = project.bdt, Makefile, configs/menuconfig/Kconfig, configs/menuconfig/.config, configs/menuconfig/config.clks.bdt, scripts/kconfig_sync.py

[targets]
default = iso
provided = configure, setup, kernel, userapps, ramdisk-root, ramdisk, disk-image, iso, run, debug, clean, clean-all
